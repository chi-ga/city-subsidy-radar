## 0. 先和大家打个招呼吧

我们是一支两人小队，围绕初赛晋级作品组队参赛。

**chiga**（队长）— 零技术背景的应届毕业生，即将去深圳入职。负责产品方向、需求定义、技术路线制定和全部前端开发。初赛阶段独立完成了覆盖北上广深 4 城的 Demo，复赛阶段主导了从 4 城到 115 城的扩展、数据懒加载改造、状态持久化等架构升级。Vibe Coding 编程年限：2 个月。

**Lingzhentaor**（队员）— 负责测试、政策文件校验和产品运营。复赛期间，Lingzhentaor 逐一校对了 115 城政策文件的准确性，对照各地人社局官网核查补贴金额、申请条件、截止时间等关键数据；同时负责匹配结果的回归测试，确保每次数据更新后核心链路不出现误匹配；在产品运营侧，负责收集用户反馈、整理高频问题，为后续迭代方向提供依据。

初赛时我用 TRAE WORK 从零做出了一个覆盖北上广深 4 城的补贴查询 Demo，那时候只是想解决自己的痛点：搞清楚自己到底能领多少人才补贴。复赛阶段，两人协作把这个 Demo 打磨成了一个覆盖全国 115 城、581 条政策的完整产品——我负责开发，Lingzhentaor 负责测试和校验，形成了"开发—校验—反馈"的协作闭环。

整个开发过程中，TRAE 既是开发工具，也是协作伙伴。我负责提出需求、审查结果、制定技术路线，TRAE 负责把想法变成可运行的代码；Lingzhentaor 则在 TRAE 生成的数据和代码基础上做交叉验证，确保政策数据与原始文件一致。作为一支零代码背景的两人小队，这种协作模式让我们在不到两个月的时间里完成了一个专业级前端项目的从 0 到 1。

## 1. 产品简介

### 是什么

城市补贴雷达是一个纯前端 Web 应用，用户输入毕业院校、学历、专业、年龄、毕业年份、落户状态、就业状态等条件后，系统自动匹配目标城市的人才补贴政策，计算预估可领金额，生成落地待办清单，并支持多城市横向对比。打开浏览器即可使用，无需注册登录。

### 面向谁

即将或刚刚跨城市就业的应届毕业生（专科/本科/硕士/博士），以及毕业 2 年内想查漏补缺的往届生。典型场景包括：拿到多个城市 offer 需要对比补贴待遇、确定目标城市后想搞清楚能领多少钱、想知道自己还差什么条件能拿更多。

### 完整功能与用户路径

产品提供三条核心查询路径，覆盖从"探索"到"决策"到"行动"的完整用户旅程：

**路径一：我能拿到什么**（单城查询）

用户选择目标城市 → 输入个人条件（院校、学历、专业、年龄、毕业年份、落户/就业状态，部分城市展开专属字段）→ 系统匹配该城市全部补贴政策 → 展示匹配结果（已匹配补贴列表 + 预估总金额 + 反向匹配"差一点就能领"的政策）→ 生成落地待办清单（材料准备、申请渠道、截止时间，支持勾选完成）→ 可选 AI 个性化解读。

[截图占位 — 单城查询：条件输入页 → 匹配结果页 → 待办清单页]

**路径二：哪个城市对我更好**（多城对比）

用户不选具体城市 → 输入个人条件 → 系统同时匹配全部 115 城补贴 → 按预估总金额排名展示 → 支持一键过滤购房补贴（避免大额房补主导排名）→ 点击任意城市可跳转到该城市的详细匹配结果。

[截图占位 — 多城对比：115 城排名列表 + 购房补贴过滤按钮]

**路径三：查看政策**（政策库浏览）

用户按城市、区域、补贴类型筛选 → 浏览全部 581 条政策的详细信息（申请条件、补贴金额、申请渠道、截止时间等）→ 政策卡片采用纸质文件风格，信息层级清晰 → 感兴趣的政策可一键收藏。

[截图占位 — 政策库：纸质文件风格卡片 + 多维筛选]

此外，产品还提供：

- **查询历史**：自动记录每次查询的条件和结果摘要，支持一键恢复历史查询条件，首页和输入页均有快捷入口
- **收藏夹**：跨页面收藏感兴趣的政策，集中查看与对比
- **城市专属字段**：深圳重点产业/STEM/拔尖计划、北京三城一区、上海临港/留学回国、广州黄埔/花都、合肥企业类型等城市特有政策适配
- **响应式设计**：桌面端首页展示 ECharts 中国地图，移动端自适应切换为列表入口

[截图占位 — 首页中国地图 + 查询历史入口 + 收藏夹页面]

### 相比初赛 Demo 的升级

| 维度 | 初赛 Demo | 复赛完整产品 |
|------|----------|-------------|
| 覆盖城市 | 4 城（北上广深） | 115 城（全国主要城市） |
| 补贴政策 | 45 条 | 581 条，全部校对完毕 |
| 院校数据 | 1,176 所 | 1,190 所（840 国内 + 350 境外） |
| 数据加载 | 静态 JSON 全量打包 | 动态 import() 按需加载 + 内存缓存 |
| 查询历史 | 无 | localStorage 持久化，指纹去重，支持恢复 |
| 收藏夹 | 无 | 跨页面收藏，持久化存储 |
| 多城对比 | 4 城对比 | 115 城对比 + 购房补贴一键过滤 |
| 结果持久化 | 无 | sessionStorage 持久化，刷新不丢失 |
| 错误边界 | 无 | ErrorBoundary 组件兜底 |
| 测试覆盖 | 少量单元测试 | 1291 个测试用例 |

## 2. 产品演示视频

[演示视频占位 —— 1-5 分钟录屏，展示完整核心流程]

## 3. 产品创作历程

### 从个人痛点到工具

这个项目始于我自己的经历。拿到深圳 offer 后，我开始研究人才补贴，发现信息分散在人社局官网、App、公众号等多个渠道。逐条核对条件非常痛苦——我虽然是 211 本科，但专业不在深圳《重点产业专业目录》里，不符合青年人才认定条件。后来又陆续了解到各区落户补贴、人才认定等不同政策，信息碎片化让整个过程充满挫败感。

初赛阶段，我从深圳一个城市的政策开始，逐步扩展到北上广深 4 城 45 条政策，跑通了"输入条件 → 匹配补贴 → 反向匹配 → 待办清单"的核心链路。那时候的目标是验证想法可行。

### 一条抖音视频带来的反馈

初赛 Demo 上线后，我把产品录屏发到了抖音。出乎意料的是，这条视频获得了大量关注和转发——评论区里挤满了应届生在问"什么时候支持杭州""成都有没有""我们城市什么时候上线"。这些留言让团队意识到，补贴信息不对称不是深圳独有的痛点，而是一个全国性的需求。很多用户甚至主动在评论区分享自己城市的补贴政策，提供了大量初赛阶段没有覆盖的城市线索。

这些来自真实用户的反馈，直接推动了复赛阶段从 4 城向 115 城的扩展决策。Lingzhentaor 在产品运营侧系统梳理了抖音评论和私信中的高频诉求，按城市需求热度排优先级，为城市扩充路线提供了依据。

[截图占位 — 抖音视频传播数据与评论区用户反馈]

### 从 4 城到 115 城的跨越

复赛最大的挑战是数据规模。从 4 城扩展到 115 城，意味着要调研、整理、录入 500 多条政策，并为每个城市适配前端表单字段。如果纯靠人工逐城操作，效率极低且容易出错。

我们用了三个策略来应对这个挑战：

第一，把城市扩充流程标准化。我和 TRAE 一起设计了一套城市扩充 SOP，从政策调研、Markdown 撰写、JSON 数据录入、前端适配到上线前审查校验，每个环节都有明确的步骤和检查点。这套 SOP 后来被封装成了 TRAE 的自定义 Skill（`add-city-subsidy`），新增一个城市时只需调用这个 Skill，就能按流程完成全部工作。

第二，把数据加载改为按需懒加载。115 个城市的政策 JSON 如果全部静态打包，初始 bundle 会非常臃肿。我改用 Vite 的 `import.meta.glob` 实现动态导入，用户查询某个城市时才加载对应数据，加载后缓存在内存中，后续访问直接读缓存。这个改动让初始加载体积大幅下降，同时保持了同步调用的开发体验。

第三，建立交叉校验机制。Lingzhentaor 在每批城市数据录入完成后，逐一对照各地人社局官网核查补贴金额、申请条件、截止时间等关键字段，确保 JSON 数据与政策原文一致。这种"开发录入 + 人工校验"的双人协作模式，让 115 城 581 条政策的整体数据准确性有了可靠保障。

### 技术架构的持续打磨

复赛期间，我在架构层面做了几次重要重构：

**数据懒加载改造**：将 `getSubsidiesByCity` 和 `getAllSubsidies` 从同步函数改为异步函数，内部用 `Map` 做内存缓存。这个改动联动更新了 `useSubsidyMatch` Hook、`Input` 页面、`Result` 页面、`Compare` 页面等 7 个文件，确保整个数据流从"同步读取"平滑过渡到"异步加载"。

**状态持久化**：给 `resultStore` 加上 Zustand 的 `persist` 中间件，将匹配结果、对比结果、排除的分类等状态存入 sessionStorage，用户刷新页面不会丢失数据。同时把 `isLoading` 和 `error` 等临时状态排除在持久化范围外，避免恢复出错误状态。

**查询历史系统**：新建 `historyStore`，用 localStorage 持久化查询记录。设计了一个基于核心字段组合的指纹去重机制，相同条件的查询只保留最新一条。存储写入时做了安全处理：配额超限自动删除最旧 5 条记录重试，避免 localStorage 写入失败导致整个应用崩溃。

**createToggle 重构**：初赛时用了一个外部工厂函数生成 toggle 逻辑，配合 `as` 类型断言。复赛时把 toggle 逻辑内联到各 store 的 action 中，消除了类型断言，代码更直接、类型更安全。

## 4. TRAE 实践过程

整个项目全程使用 TRAE WORK 和 TRAE IDE 开发。我前期用 TRAE WORK 搜集整理政策文件、做数据审查；搭框架和写匹配逻辑时用 TRAE IDE，能直接看到代码更有安全感；后期做审查和调整工作时又转回 TRAE WORK，因为移动端可以随时布置任务。Lingzhentaor 主要使用 TRAE WORK 辅助政策校验工作——用 TRAE 快速比对政策原文与录入数据的关键字段差异，提高校验效率。

### 4.1 开发流程概览

1. **项目初始化**：用 TRAE 创建 React 19 + TypeScript + Vite 项目，配置 Tailwind CSS、React Router DOM 7、Zustand、ECharts 和 Vitest。
2. **页面骨架搭建**：首页（ECharts 中国地图 + 三路径入口）、条件输入页、结果分析页、城市对比页、政策库页、收藏夹页、查询历史页、设置页，全部路由懒加载。
3. **数据整理与录入**：115 城 581 条补贴政策整理为统一 Schema 的 JSON，补充 1,190 所院校、805 个本科专业、深圳 255 个重点产业专业、199 个拔尖计划基地等数据。
4. **规则匹配引擎**：在 `matcher.ts` 中实现核心匹配逻辑，包括金额计算（一次性/月/季/年）、多通道认定（`criterionSets`）、互斥组取最高值、反向匹配和待办清单生成。
5. **数据懒加载改造**：用 Vite `import.meta.glob` 替换静态 JSON 导入，`Map` 内存缓存，异步加载函数。
6. **状态持久化**：`resultStore` 用 sessionStorage 持久化匹配结果，`historyStore` 用 localStorage 持久化查询历史，`favoritesStore` 用 localStorage 持久化收藏。
7. **城市扩充 SOP**：设计并封装 `add-city-subsidy` 自定义 Skill，标准化城市扩充全流程。
8. **测试与验收**：1291 个 Vitest 测试用例覆盖匹配引擎、金额计算、互斥组、多通道认定等核心逻辑。

### 4.2 自定义 Skill：城市扩充工作流

复赛期间最具创新性的 TRAE 实践是创建了 `add-city-subsidy` 自定义 Skill。这个 Skill 把新增一个城市的完整流程封装成了一套可复用的标准操作：

- **政策调研**：指导如何搜集目标城市的人才补贴政策文件
- **Markdown 撰写**：将政策信息整理为结构化的 Markdown 文档
- **JSON 数据录入**：按统一 Schema 将政策转为前端可用的 JSON 格式
- **前端适配**：更新 `constants.ts`（CityCode 类型、CITY_NAMES）、`data/index.ts`（城市列表、市级 location）、`city-conditions.json`（城市条件配置）、地图组件（COVERED_CITIES 与 GeoJSON 匹配）、对比页城市列表
- **上线前审查**：校验数据准确性、条件匹配逻辑、前端显示一致性

这个 Skill 让城市扩充从一个需要多步手动操作、容易遗漏环节的复杂任务，变成一个可标准化、可复制的流程。115 个城市中，大部分都是通过这个 Skill 完成数据录入和前端适配的。

### 4.3 开发关键步骤截图

> 以下截图位置预留，正式发帖时补充。

**截图 1：使用 add-city-subsidy Skill 扩充新城市**

[截图占位]

**截图 2：数据懒加载改造 — 动态 import 与内存缓存**

[截图占位]

**截图 3：查询历史系统 — localStorage 持久化与指纹去重**

[截图占位]

**截图 4：状态持久化 — resultStore + persist 中间件**

[截图占位]

**截图 5：115 城政策数据校对与匹配逻辑修正**

[截图占位]

### 4.4 关键任务 Session ID

| 任务 | Session ID |
|------|------------|
| [任务 1：使用 add-city-subsidy Skill 扩充新城市] | [Session ID 留空] |
| [任务 2：数据懒加载改造] | [Session ID 留空] |
| [任务 3：查询历史系统实现] | [Session ID 留空] |
| [任务 4：resultStore 持久化改造] | [Session ID 留空] |
| [任务 5：115 城政策数据校对] | [Session ID 留空] |

### 4.5 一个印象深刻的踩坑点

在扩展到 115 城的过程中，南通市的购房补贴出了问题。南通市区和下辖县级市各有购房补贴政策，且互斥——同一个人只能申领其中一份。最初数据中没有给这两条政策设置 `exclusiveGroup` 字段，导致匹配引擎把它们当作独立政策同时计入总额，用户看到的结果比实际能领的金额高出一倍。

这个问题是 Lingzhentaor 在校验南通政策数据时发现的——对照人社局原文确认互斥关系后，立刻反馈给我。我和 TRAE 一起排查了全部 115 城的数据，给所有存在互斥关系的政策补上了 `exclusiveGroup` 分组字段。匹配引擎在汇总金额时，同一 `exclusiveGroup` 内只取金额最高的一条。这次修复让我们意识到，数据规模扩大后，Schema 设计中的每一个字段都可能影响匹配准确性，互斥组、多通道认定这些结构性约束必须从数据层面严格定义——而人工校验环节正是发现这类结构性遗漏的最后一道防线。

## 5. 技术方案分享

### 技术栈

| 层级 | 技术 |
|------|------|
| 前端框架 | React 19 + TypeScript |
| 构建工具 | Vite |
| 路由 | React Router DOM 7 |
| 状态管理 | Zustand（+ persist 中间件） |
| CSS 框架 | Tailwind CSS |
| 图表/地图 | ECharts |
| 测试 | Vitest + Testing Library |

### 架构特点

**纯前端 SPA，无后端依赖**。全部补贴数据以静态 JSON 内嵌，规则匹配引擎在前端执行，用户输入的条件不会离开浏览器。这个架构选择降低了部署成本（静态托管即可），也保护了用户隐私。

**数据按需加载**。115 个城市的政策 JSON 通过 Vite `import.meta.glob` 注册为动态导入入口，用户查询某城市时才发起加载请求，加载结果缓存在 `Map` 中。初始 bundle 只包含应用框架代码，不包含任何城市数据。

**三层持久化策略**：

| 存储层 | 介质 | 内容 | 生命周期 |
|--------|------|------|----------|
| 临时状态 | 内存 | isLoading、error | 页面刷新即失 |
| 会话状态 | sessionStorage | 匹配结果、对比结果、排除分类 | 标签页关闭即失 |
| 持久状态 | localStorage | 查询历史、收藏夹、AI 配置 | 永久保留 |

`resultStore` 的 `partialize` 选项精确控制了哪些字段需要持久化——`isLoading` 和 `error` 被排除在外，避免刷新后恢复出"加载中"或"报错"的脏状态。

**规则匹配引擎**。`matcher.ts` 是核心模块，处理三类逻辑：

- **金额计算**：区分一次性、按月、按季、按年发放的补贴，按用户学历档位计算实际可领金额
- **多通道认定**：部分政策提供多条认定路径（如深圳青年人才认定的 8 条路径），用 `criterionSets` 结构表达"任一满足即匹配"的语义，引擎逐组判断后汇总
- **互斥组处理**：同一 `exclusiveGroup` 内的政策只取金额最高的一条，避免重复计算

### 测试覆盖

1291 个 Vitest 测试用例覆盖匹配引擎的核心逻辑，包括金额计算（一次性/月/季/年混算）、互斥组取最高值、多通道认定（全满足/部分满足/全不满足）、反向匹配过滤逻辑（学历/专业等不可变条件自动排除）等场景。除自动化测试外，Lingzhentaor 负责手动回归测试——每次数据更新或匹配逻辑调整后，用一组覆盖不同学历、城市、区域的典型用户画像逐一验证匹配结果，确保核心链路不出现误匹配。

## 6. 社会价值分析

每年有上千万高校毕业生跨城市就业，人才补贴是城市吸引年轻人的重要政策手段。但信息不对称导致大量补贴无人申领——不是年轻人不关心钱，而是获取这些信息的成本太高。

城市补贴雷达试图降低的不是补贴金额本身，而是信息获取的门槛。一个应届生要搞清楚自己能领多少补贴，传统路径是：搜索目标城市人社局官网 → 逐条阅读政策文件 → 对照自身条件判断是否符合 → 记录申请材料和截止时间 → 如果有多个城市 offer 还要重复以上流程。这个过程中任何一个环节出错——漏看一条政策、误解一个条件、记错一个截止日期——都可能导致实打实的经济损失。

这个工具把上述流程压缩到了几秒钟：输入条件，一键匹配。更重要的是反向匹配功能——它不只告诉你"能领什么"，还告诉你"差什么能领更多"，并且自动过滤掉学历、专业等无法改变的硬条件，只推荐落户、社保等你还能补齐的项。这让补贴查询从"被动确认"变成了"主动规划"。

从 4 城扩展到 115 城，意味着覆盖了全国绝大多数应届生可能去的城市。一个在武汉读大学、拿到深圳 offer 的学生，可以同时对比深圳、武汉、以及老家所在城市的补贴待遇，做出更全面的决策。这种跨城市的信息平权，本身就是社会公平的一种技术实现。

产品在抖音上的传播也印证了这一需求的真实性。大量应届生在评论区主动分享自己城市的补贴政策、催促新增城市覆盖——用户不再是被动的内容消费者，而是在主动参与一个政策信息的共建过程。这种自发参与本身就是对"信息平权"最有力的注脚。

## 7. 产品迭代规划

- **政策更新提醒**：补贴政策每年都在变，计划加入政策变动通知功能
- **AI 个性化解读**：基于用户匹配结果，调用大模型生成个性化的申请建议和避坑提示
- **更多城市覆盖**：持续扩展至全国所有地级市
- **申请进度跟踪**：从"告诉你能领什么"延伸到"帮你跟踪申请进度"

## 8. 对应的初赛晋级作品链接

**初赛帖**：[【社会服务赛道】城市补贴雷达 —— 应届毕业生人才补贴智能匹配工具](https://forum.trae.cn/t/topic/51843)

城市补贴雷达从初赛的 4 城 Demo 成长为覆盖 115 城的完整产品。如果你也是应届生，或者你所在城市有值得补充的补贴政策，欢迎在评论区留言交流。
